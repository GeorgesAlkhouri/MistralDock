# SonarQube Cloud automatic-analysis configuration.
# Keep the default scope so Dockerfiles and GitHub Actions workflows are analyzed too.
sonar.python.version=3.13
sonar.tests=tests
sonar.sourceEncoding=UTF-8
